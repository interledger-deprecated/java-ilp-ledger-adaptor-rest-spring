machine:
  java:
    version: oraclejdk8

checkout:
  post:
    # we rely on the source of several other ILP projects for now. 
    # TODO: once we reach a release milestone, swap this for dependecies on published jars
    - git clone https://github.com/interledger/java-crypto-conditions
    - git clone https://github.com/interledger/java-ilp-core
    - git clone https://github.com/interledger/java-ilp-client

test:
  post:
    # see https://circleci.com/docs/test-metadata/#gradle-junit-results
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \