machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    # we rely on the source of several other ILP projects for now. 
	# TODO: once we reach a release milestone, swap this for dependecies on published jars
	- git clone git@github.com: https://github.com/interledger/java-crypto-conditions
	- git clone git@github.com: https://github.com/interledger/java-ilp-core
	- git clone git@github.com: https://github.com/interledger/java-ilp-client
